download the tar file from the site 
extract
  sudo tar -xvzf mysql-shell-8.x.x-linux-glibc2.12-x86_64.tar.gz -C /opt
  sudo mv /opt/mysql-shell-8.x.x-linux-glibc2.12-x86_64 /opt/mysql-shell
  sudo ln -s /opt/mysql-shell/bin/mysqlsh /usr/local/bin/mysqlsh
