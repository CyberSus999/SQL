Think of it a folder with files(tables)
  --it is an organized collection of data

Create one
  CREATE DATABASE dbname;
Delete one
  DROP DATABSE dbname;
Use one
  USE dname;

ALTERING
readonly mode
  ALTER DATABASE dbname READ ONLY = 1;
  1 = enabled , 0 = disabled
egg lets add a phone number to the table below
  ALTER TABLE tablename
  ADD phone_number VARCHAR()
rename a cloumn
  ALTER TABLE  tablename
  RENAME COLUMN  columnname TO newname;
modify a column
  ALTER TABLE tablename
  MODIFY COLUMN columnname VARCHAR() or new datatype
move column
  ALTER TABLE tablename
  MODIFY columnname datatype()
  POSITION;
  eg
  FIRST
  AFTER colunmname
drop a column
  ALTER TABLE tablename
  DROP COLUMN columnname;
MAKING TABLES
eg employee data
CREATE TABLE employee(
  id INT,
  first_name VARCHAR(50),
  last_name VARCHAR(50),
  hourly_pay DECIMAL(5, 2),
  hire_date DATE
);
datatypes
INT
VARCHAR()
DECIMAL(digits, decimal places)
DATE/DATETIME()

SELECTING FROM TABLES
SELECT * FROM tablename ; #select everything
SELECT column_name(s) FROM table_name WHERE condition;
  eg. SELECT name FROM users WHERE age = 25;
SELECT * FROM users WHERE age = 25;
SELECT name, age FROM users WHERE age > 18 AND city = 'Gabs';

RENAME TABLE tablename TO newname;

INSERTING A ROW
INSERT INTO tablename 
VALUES (follow the order of the columns when adding info);
  eg the employee table we made up there
  VALUES(id, firstname, lastname .....)pay attention to the datatypes ;
  eg VALUES(1, "Bob", "Ate", "25.50", "year-month-day");
for multiple valuues
VALUES (),
       (),
       ();
  if you dont wanna add all the details for your row
  INSERT INTO tablename (columnnames you want to insert)
  VALUES();

UPDATING A COLUMN
UPDATE tablename
SET columnname = something eg SET name = "bob"
WHERE position;  eg WHERE id = 3;

DECRIBE tablename; # this command decribes the table

AUTOCOMMIT, COMMIT, ROLLBACK
SET AUTOCOMMIT = ON/OFF;
COMMIT; #make a save point
ROLLBACK; # RESTORE savepoint

DAT and TIME 

CREATE TABLE test(
    my_date DATE,
    my_time TIME,
    my_datime DATETIME
);
INSERT INTO test
VALUES(CURRENT_DATE(), CURRENT_TIME(), NOW()); #return current date
#CURRENT_DATE +/- 1 .....in/de-crements the date by 1 aka tommorrow

UNIQUE contraint
-it ensures all values in a column are all different
 add it during table making
CREATE TABLE products(
  product_id INT,
  product_Name VARCHAR(20) UNIQUE,
  price DECIMAL(4, 2);
);
  add after
ALTER TABLE tablename
ADD CONSTRAINT
UNIQUE(columnname);

NOT NULL constraint
--any vlaue that should not be null
same way as before but;
ALTER TABLE tablename
MODIFY columnname datatype() NOT NULL;
